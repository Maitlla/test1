//Ejercicios examen Javascript 21/03/2022

/*
const elementos = [`plátanos`, `manzanas`, `ciruelas`];
const lista = document.createElement(`ul`);
const elementosli = elementos.map(
    (elemento)=>{
        const li = document.createElement(`li`);
        li.innerText = elemento;
        return li;
    }
)
lista.appendChild(...elementosLi);
const body = document.querySelector(`body`);
body.appendChild(lista);
*/
       
/*
const elementos = [`plátanos`, `manzanas`, `ciruelas`];
const lista = document.createElement(`ul`);
const elementosli = elementos.map(
    (elemento)=>{
        const li = document.createElement(`li`);
        li.innerText = elemento;
        return li;
    }
)
lista.append(...elementosLi);
const body = document.querySelector(`body`);
body.appendChild(lista);
*/

/*const parrafo = document.createElement("p");
parrafo.innerText = "Hola, ¿qué haces?";
const body = document.querySelector("body");
body.appendChild(parrafo);
*/

/*
const parrafo = document.createElement("p");
parrafo.innerText = "Hola, ¿qué haces?";
const body = document.querySelector("body");
body.innerText = parrafo;
*/